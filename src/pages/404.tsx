
export default function ErrorPage() {
    // const error = useRouteError();

    return (
        <div id="error-page">
            <h1>Oops!</h1>
            <p>Случилась ошибка, мы уже над этим работаем...</p>
            {/*<p>*/}
            {/*    <i>{error.statusText || error.message}</i>*/}
            {/*</p>*/}
        </div>
    );
}
